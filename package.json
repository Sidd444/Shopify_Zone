{
  "name": "shopify-zone",
  "version": "1.0.0",
  "description": "Shopify Zone Ecommerce application",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
